# Clé API Alpha Vantage (optionnelle)
# Vous pouvez configurer cette clé de deux manières :
#   1. Via l'interface utilisateur : bouton "🔑 Configurer les APIs" (recommandé, prioritaire)
#   2. Via ce fichier .env (fallback si la clé n'est pas dans l'interface)
#
# Pour obtenir une clé gratuite (25 requêtes/jour) : https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=ta_cle_ici
